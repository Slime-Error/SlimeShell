// Get a reference to the output <pre> element
const outputElement = document.getElementById('output');

// Define a function to write output to the <pre> element
function writeOutput(text) {
  outputElement.innerText += text + '\n';
}

// Example script to log some output and write it to the console
writeOutput('Hello, welcome to Slimeshell.\nI am the example script writting output here.\nMany things are planned to be added to SlimeShell and development will be quick with the help of premium CHATGTP support. Currently features in progress are: \n\nQUICK NOTES, CONSOLE SCRIPTS, DIRECTORY NAVIGATION AND OPENING, SFX, APPLICATION FILTER VISUAL FX');
